[package]
name = "codec-policy"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
ts-rs = { version = "11", features = ["serde-compat"] }

