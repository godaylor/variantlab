[package]
name = "variant-engine"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
creative-engine = { version = "0.1.0", path = "../creative-engine" }
serde_json = "1.0.145"
sha2 = "0.10.9"
studio-model = { version = "0.1.0", path = "../studio-model" }
thiserror = "2.0.18"
ts-rs = { version = "11", features = ["serde-compat"] }
validation-engine = { version = "0.1.0", path = "../validation-engine" }
